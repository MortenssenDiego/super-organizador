import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.superorganizador.app',
  appName: 'super-organizador',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
